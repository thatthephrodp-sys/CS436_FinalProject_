# 📖 คู่มือการใช้งาน (User Manual)
**Project:** Simple Calculator with History & Themes
**Version:** 1.0

## 1. บทนำ (Introduction)
ระบบเครื่องคิดเลขพื้นฐานที่ได้รับการออกแบบมาเพื่อความสะดวกสบายในการใช้งาน รองรับการคำนวณทางคณิตศาสตร์พื้นฐาน พร้อมฟังก์ชันพิเศษเช่น การดูประวัติย้อนหลัง, การเปลี่ยนธีม (Dark/Light Mode), และระบบแสดงผลลัพธ์ล่วงหน้า (Live Preview)

## 2. วิธีการใช้งานเบื้องต้น (Basic Usage)
### การคำนวณพื้นฐาน
1. **ปุ่มตัวเลข (0-9):** กดเพื่อระบุค่าที่ต้องการ
2. **ปุ่มดำเนินการ (+, -, ×, ÷):** กดเพื่อเลือกวิธีการคำนวณ
3. **ปุ่มเท่ากับ (=):** กดเพื่อสรุปผลลัพธ์สุดท้าย
4. **ปุ่มลบ (DEL):** กดเพื่อลบตัวเลขทีละหลัก
5. **ปุ่มล้างค่า (AC):** กดเพื่อรีเซ็ตค่าทั้งหมดเป็น 0

### การใช้ฟังก์ชันพิเศษ
* **Live Preview:** เมื่อท่านพิมพ์โจทย์ (เช่น `10 + 20`) ระบบจะแสดงผลลัพธ์จางๆ ด้านบนทันทีโดยไม่ต้องกด `=` ช่วยให้ตรวจสอบความถูกต้องได้ก่อนยืนยัน
* **Percentage (%):** ใช้คำนวณเปอร์เซ็นต์ เช่น `50` กด `%` จะได้ `0.5`

## 3. ระบบประวัติการคำนวณ (Calculation History)
ไม่ต้องกลัวลืมผลลัพธ์เก่า! ท่านสามารถดูย้อนหลังได้ดังนี้:
1. กดปุ่มไอคอน **นาฬิกา (🕒)** ที่มุมซ้ายบน
2. หน้าต่างประวัติจะเลื่อนลงมา แสดงรายการคำนวณ 10 รายการล่าสุด
3. กดปุ่ม **"Clear History"** หากต้องการลบประวัติทั้งหมด
4. กดปุ่มนาฬิกาอีกครั้งเพื่อปิดหน้าต่าง

## 4. การปรับแต่งธีม (Theme Customization)
เลือกโหมดที่สบายตาสำหรับท่าน:
* **โหมดกลางวัน (Light Mode):** กดปุ่มไอคอน **พระอาทิตย์ (☀️)** เพื่อเปลี่ยนเป็นพื้นหลังสีขาว ตัวอักษรสีดำ
* **โหมดกลางคืน (Dark Mode):** กดปุ่มไอคอน **พระจันทร์ (🌙)** เพื่อเปลี่ยนเป็นพื้นหลังสีดำ ถนอมสายตา

## 5. การจัดการข้อผิดพลาด (Troubleshooting)
* **Error Division by Zero:** หากมีการหารด้วยศูนย์ ระบบจะแจ้งเตือนว่า `Error` และหยุดการทำงาน ให้กด `AC` เพื่อเริ่มใหม่